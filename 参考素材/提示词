帮我撰写《水面目标精准定位技术报告》，使用 poi-tl-project 工具生成 .docx 文件。

## 工具

使用 poi-tl-project 项目（C:\Users\deer\Desktop\Projects\poi-tl-project）：
- poi-tl（Java）：主力工具，模板填充，样式保留好
- python-docx（Python）：备选工具，用于补充动态内容

本报告使用 poi-tl（模板填充），原因：
- 有参考文档（水面目标精准定位技术报告.docx）可作为模板
- 样式保留好，与参考文档格式一致
- 复杂表格和公式支持更好
- 批量生成效率高

备选：如果poi-tl无法满足某些动态内容需求，降级为python-docx补充

## 参考文档

报告结构和格式参考：`水面目标精准定位技术报告.docx`（当前目录）

### 参考文档分析步骤

1. 读取 `水面目标精准定位技术报告.docx` 的元信息（字体、字号、行距、页边距）
2. 提取标题层级结构（一级/二级/三级标题的样式）
3. 记录图表编号方式（图X-Y、表X-Y格式）
4. 记录页眉页脚内容和格式
5. 将提取结果保存为格式规范，供生成时使用

## 素材来源

### 文件素材（主要来源）

所有素材以文件形式存储，确保可追溯：

| 类别 | 路径 | 优先级 | 内容 |
|------|------|--------|------|
| 论文笔记 | `paper/notes.md` | P0 | 论文核心内容、技术路线、公式 |
| 参考框架 | `paper/gemini_reference_ch1_4.md` | P0 | 前4章参考文本 |
| 论文OCR | `paper/paper_markdown.md` | P1 | 论文全文OCR文本 |
| 论文原文 | `paper/*.pdf` | P1 | CVPR 2026 PDF |
| 实验结果 | `code/Result/` | P0 | 6组实验pkl数据 |
| 项目上下文 | `日志/CONTEXT.md` | P0 | 技术路线、实验结果汇总 |
| 经验教训 | `日志/LEARNINGS.md` | P2 | 可复用经验 |
| 代码配置 | `code/config.yaml` | P1 | 实验配置 |
| 区域参数 | `code/Regions_params/QZ_Town.yaml` | P1 | 青州古镇配置 |
| 数据元数据 | `code/Data/metadata/QZ_Town.json` | P1 | 无人机元数据 |
| 项目图片 | `code/assets/*.png` | P2 | overview、reference_map等 |
| 参考报告 | `水面目标精准定位技术报告.docx` | P0 | 格式和结构参考 |

### pkl数据处理说明

1. 先读取一个pkl样本，确认数据结构
2. 确认字段名：pred_error, location_error_list, BLH_list, inliners_list等
3. 确认A@K的计算公式：A@K = mean(pred_error < K) * 100
4. 如果字段名不符，更新处理脚本

## 报告结构（参照 水面目标精准定位技术报告.docx）

- 第1章：研究背景与任务需求
- 第2章：水面目标精准定位问题分析
- 第3章：基于参考地图匹配的视觉定位总体框架
- 第4章：水面目标地理坐标解算方法
- 第5章：实验与结果分析（从pkl提取数据，计算A@5m、A@10m、A@20m）
- 第6章：结论与展望

## 输出

输出到当前项目根目录：`水面目标精准定位技术报告_生成版.docx`

## 错误处理

- 参考文档不存在 → 终止并告知用户
- pkl文件读取失败 → 跳过该实验，标记为"数据缺失"
- poi-tl-project不可用 → 降级到python-docx
- 子代理执行超时（>5min） → 标记为FAIL，记录超时原因
- 循环超过3轮仍未全部PASS → 停止，输出当前版本和剩余问题清单

## 降级策略

如果无法提取参考文档格式：
- 使用默认格式：宋体/小四/1.5倍行距/A4纸
- 标题格式：一级标题黑体三号，二级标题黑体四号，三级标题黑体小四
- 页边距：上下2.54cm，左右3.17cm

## 中间产物

- 每轮生成的报告保存为：`水面目标精准定位技术报告_生成版_v{n}.docx`
- 验证结果保存为：`验证报告_v{n}.md`
- 最终版本重命名为：`水面目标精准定位技术报告_生成版.docx`

## 文件大小控制

- 图片分辨率：不超过300dpi
- 图片格式：优先使用PNG（图表）或JPEG（照片）
- 单张图片大小：不超过2MB
- 最终报告大小：不超过50MB

## 验证机制（使用6个并行子代理）

### 执行约束

- /loop指令每次迭代为一个完整对话轮次
- 6个子代理使用Agent工具并行启动
- 子代理结果通过Agent工具返回，主代理汇总
- 每轮迭代后，主代理决定是否继续循环

### 子代理验证流程

1. **生成报告后**，主代理提取报告摘要（章节标题、段落数量、表格数据、公式内容、字数统计）
2. 启动6个子代理并行验证（子代理基于文本摘要验证，无需读取.docx）
3. 每个子代理返回验证结果和问题清单
4. 主代理汇总所有子代理的问题清单
5. 如有问题，主代理修复后重新生成
6. 重复步骤1-5直到所有子代理都返回 PASS 或达到最大轮数

### 子代理返回格式

每个子代理必须返回以下JSON格式：
```json
{
  "agent": "子代理名称",
  "status": "PASS" | "FAIL",
  "issues": [
    {
      "severity": "critical" | "major" | "minor",
      "location": "第X章/第X节",
      "description": "问题描述",
      "suggestion": "修复建议"
    }
  ],
  "summary": "一句话总结"
}
